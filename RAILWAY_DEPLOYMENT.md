# 🚂 Деплой на Railway - Пошаговая инструкция

## Шаг 1: Создайте аккаунт на Railway

1. Перейдите на **https://railway.app**
2. Нажмите **"Start a New Project"** или **"Login with GitHub"**
3. Авторизуйтесь через GitHub

## Шаг 2: Создайте новый проект

1. В панели Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий **`grytsayo/french-course-platform`**
4. Выберите ветку **`claude/payment-backend-setup-011CUjMTJLCoQw2m6fCQ1vGo`**

## Шаг 3: Добавьте PostgreSQL базу данных

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных и добавит переменные окружения

## Шаг 4: Настройте переменные окружения

В разделе **"Variables"** вашего сервиса добавьте:

### Обязательные переменные:

```
NODE_ENV=production
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
```

### Переменные базы данных (автоматически):
Railway автоматически добавит:
- `DATABASE_URL` - полный URL подключения к PostgreSQL

Вам нужно добавить дополнительно (Railway предоставит значения из DATABASE_URL):
```
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
```

### Платежные системы (добавьте позже):
```
YUKASSA_SHOP_ID=your_shop_id
YUKASSA_SECRET_KEY=your_secret_key
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
```

### Email (добавьте позже):
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=French Course <noreply@french-course.com>
```

## Шаг 5: Настройте BASE_URL

После деплоя Railway даст вам URL типа `your-app.up.railway.app`

Добавьте переменную:
```
BASE_URL=https://your-app.up.railway.app
```

## Шаг 6: Запустите миграции

Railway автоматически установит зависимости и запустит приложение.

Чтобы запустить миграции и заполнить базу:

1. Откройте **вкладку вашего сервиса** в Railway
2. Перейдите в **"Settings"** → **"Deploy Triggers"**
3. Или используйте Railway CLI:

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Залогиньтесь
railway login

# Свяжите с проектом
railway link

# Запустите миграции
railway run npm run migrate

# Заполните базу данными
railway run npm run seed
```

### Или через веб-интерфейс:

1. В проекте перейдите в раздел вашего сервиса
2. Найдите вкладку **"Deployments"**
3. Нажмите на последний деплой
4. Нажмите **"View Logs"**
5. Если нужно запустить команды, используйте Railway CLI

## Шаг 7: Проверьте деплой

После успешного деплоя:

1. Railway покажет URL вашего приложения (например: `https://your-app.up.railway.app`)
2. Откройте:
   - `https://your-app.up.railway.app/index.html` - главная страница
   - `https://your-app.up.railway.app/api/health` - проверка API

## Шаг 8: Настройте домен (опционально)

1. В настройках сервиса найдите **"Settings"** → **"Domains"**
2. Можете добавить свой домен или использовать стандартный Railway домен

## 🔧 Важные моменты:

### Автоматический деплой:
Railway автоматически делает редеплой при каждом push в ветку на GitHub!

### Логи:
Смотрите логи в реальном времени в разделе **"Deployments"** → **"View Logs"**

### Стоимость:
- **$5/месяц** бесплатно для начинающих
- После этого ~$5-10/месяц в зависимости от нагрузки

### База данных:
PostgreSQL на Railway включает:
- Автоматические бэкапы
- Monitoring
- Автомасштабирование

## ⚠️ Важно после деплоя:

1. **Смените JWT_SECRET** на случайный длинный ключ
2. **Добавьте реальные ключи** платёжных систем
3. **Настройте email** для отправки кодов доступа
4. **Настройте webhooks** для платёжных систем:
   - ЮKassa webhook: `https://your-app.up.railway.app/api/payments/yukassa/webhook`
   - Stripe webhook: `https://your-app.up.railway.app/api/payments/stripe/webhook`

## 🎉 Готово!

После всех шагов у вас будет:
- ✅ Полноценный сайт в интернете
- ✅ Backend работает
- ✅ PostgreSQL база данных
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат (HTTPS)

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Railway
2. Убедитесь что все переменные окружения установлены
3. Проверьте что миграции выполнены
4. Проверьте подключение к базе данных

## Полезные ссылки:

- Railway документация: https://docs.railway.app
- Ваш проект: https://railway.app/project/[your-project-id]
