# French Course Platform - Общайся легко на Лазурке! 🇫🇷

Платформа для онлайн-курса французского языка для туристов на Французской Ривьере.

## Возможности

- 💳 **Интеграция платежей**: ЮKassa и Stripe
- 📧 **Автоматическая отправка email** с кодами доступа
- 🔐 **Аутентификация пользователей** через JWT
- 📚 **Управление курсами и уроками**
- 📊 **Отслеживание прогресса** обучения
- 🗄️ **PostgreSQL база данных**

## Технологии

### Backend
- Node.js + Express
- PostgreSQL
- JWT для аутентификации
- Nodemailer для email
- YooKassa & Stripe API

### Frontend
- HTML5, CSS3, JavaScript (Vanilla)
- Адаптивный дизайн

## Установка

### Требования
- Node.js >= 16.x
- PostgreSQL >= 13.x
- npm или yarn

### Шаги установки

1. **Клонируйте репозиторий**
```bash
git clone <your-repo-url>
cd french-course-platform
```

2. **Установите зависимости**
```bash
npm install
```

3. **Создайте базу данных PostgreSQL**
```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE french_course;
```

4. **Настройте переменные окружения**
```bash
# Скопируйте файл с примером
cp .env.example .env

# Отредактируйте .env и добавьте свои ключи
nano .env
```

Обязательно настройте:
- `DB_*` - параметры подключения к PostgreSQL
- `JWT_SECRET` - секретный ключ для JWT токенов
- `YUKASSA_*` - ключи YooKassa (получите на https://yookassa.ru)
- `STRIPE_*` - ключи Stripe (получите на https://stripe.com)
- `SMTP_*` - настройки почты (Gmail, SendGrid и т.д.)

5. **Запустите миграции базы данных**
```bash
npm run migrate
```

6. **Заполните базу данных тестовыми данными**
```bash
npm run seed
```

7. **Запустите сервер**

Для разработки (с автоперезагрузкой):
```bash
npm run dev
```

Для production:
```bash
npm start
```

Сервер запустится на http://localhost:3000

## Структура проекта

```
french-course-platform/
├── server/
│   ├── database/
│   │   ├── db.js              # Подключение к БД
│   │   ├── schema.sql         # Схема БД
│   │   ├── migrate.js         # Скрипт миграций
│   │   └── seed.js            # Тестовые данные
│   ├── routes/
│   │   ├── auth.js            # Маршруты аутентификации
│   │   ├── payments.js        # Маршруты платежей
│   │   ├── courses.js         # Маршруты курсов
│   │   └── users.js           # Маршруты пользователей
│   ├── middleware/
│   │   └── auth.js            # JWT middleware
│   ├── services/
│   │   └── emailService.js    # Сервис отправки email
│   ├── utils/
│   │   └── helpers.js         # Вспомогательные функции
│   └── index.js               # Главный файл сервера
├── *.html                      # Frontend страницы
├── package.json
├── .env.example
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход по email и коду доступа
- `GET /api/auth/verify` - Проверка JWT токена
- `POST /api/auth/request-access` - Запрос кода доступа на email

### Платежи
- `POST /api/payments/yukassa/create` - Создание платежа YooKassa
- `POST /api/payments/yukassa/webhook` - Webhook YooKassa
- `POST /api/payments/stripe/create` - Создание платежа Stripe
- `POST /api/payments/stripe/webhook` - Webhook Stripe
- `GET /api/payments/status/:paymentId` - Статус платежа

### Курсы (требуется авторизация)
- `GET /api/courses/:courseId` - Информация о курсе
- `GET /api/courses/my/enrollments` - Мои курсы
- `GET /api/courses/:courseId/lessons` - Все уроки курса с прогрессом
- `GET /api/courses/lessons/:lessonId` - Содержимое урока
- `POST /api/courses/lessons/:lessonId/progress` - Обновить прогресс

### Пользователи (требуется авторизация)
- `GET /api/users/profile` - Профиль пользователя
- `PUT /api/users/profile` - Обновить профиль
- `GET /api/users/stats` - Статистика обучения

## Настройка платежей

### ЮKassa (YooKassa)

1. Зарегистрируйтесь на https://yookassa.ru
2. Создайте магазин
3. Получите `shopId` и `Секретный ключ`
4. Добавьте в `.env`:
```env
YUKASSA_SHOP_ID=ваш_shop_id
YUKASSA_SECRET_KEY=ваш_секретный_ключ
```
5. Настройте webhook URL: `https://your-domain.com/api/payments/yukassa/webhook`

### Stripe

1. Зарегистрируйтесь на https://stripe.com
2. Получите API ключи в Dashboard
3. Добавьте в `.env`:
```env
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
```
4. Настройте webhook URL: `https://your-domain.com/api/payments/stripe/webhook`

## Настройка Email

### Gmail

1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
3. Добавьте в `.env`:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш@gmail.com
SMTP_PASS=пароль_приложения
```

### SendGrid (рекомендуется для production)

1. Зарегистрируйтесь на https://sendgrid.com
2. Создайте API ключ
3. Добавьте в `.env`:
```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=ваш_api_ключ_sendgrid
```

## Deployment

### Heroku

```bash
# Установите Heroku CLI
heroku login

# Создайте приложение
heroku create your-app-name

# Добавьте PostgreSQL
heroku addons:create heroku-postgresql:hobby-dev

# Установите переменные окружения
heroku config:set JWT_SECRET=your_secret
heroku config:set YUKASSA_SHOP_ID=your_id
# ... остальные переменные

# Деплой
git push heroku main

# Запустите миграции
heroku run npm run migrate
heroku run npm run seed
```

### Railway / Render

1. Подключите GitHub репозиторий
2. Добавьте PostgreSQL базу данных
3. Настройте переменные окружения
4. Добавьте build команды:
   - Build: `npm install`
   - Start: `npm start`
5. Запустите миграции вручную через консоль

## Безопасность

⚠️ **Важно для production:**

1. Измените `JWT_SECRET` на длинный случайный ключ
2. Используйте HTTPS для всех запросов
3. Настройте CORS правильно
4. Храните `.env` в безопасности (не коммитьте в git)
5. Регулярно обновляйте зависимости
6. Проверьте webhook подписи от платежных систем
7. Ограничьте частоту запросов (rate limiting)

## Разработка

### Локальная разработка

```bash
# Запустить в режиме разработки
npm run dev

# Сервер будет на http://localhost:3000
# Откройте http://localhost:3000/index.html
```

### Тестирование платежей

Используйте тестовые карты:

**Stripe тестовые карты:**
- Успешная оплата: `4242 4242 4242 4242`
- Отклонена: `4000 0000 0000 0002`

**ЮKassa тестовые карты:**
- См. документацию: https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи: `heroku logs --tail` (на Heroku)
2. Убедитесь, что все переменные окружения настроены
3. Проверьте подключение к БД
4. Проверьте email конфигурацию

## Лицензия

MIT License

## Контакты

Для вопросов и предложений свяжитесь с нами через email или создайте issue на GitHub.

---

Made with ❤️ for French language learners on the Côte d'Azur
