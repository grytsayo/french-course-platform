# 💻 Инструкция: Запуск проекта локально

## Предварительные требования:

Убедитесь, что у вас установлено:
- ✅ Node.js (версия 16 или выше)
- ✅ PostgreSQL (версия 13 или выше)
- ✅ npm (обычно идёт вместе с Node.js)

Проверьте установку:
```bash
node --version   # Должна быть >= 16.x
npm --version    # Должна быть >= 8.x
psql --version   # Должна быть >= 13.x
```

---

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

⏳ Подождите 1-2 минуты - установятся все необходимые пакеты.

---

## Шаг 2: Настройка базы данных

### На Mac/Linux:

```bash
# Запустите PostgreSQL
brew services start postgresql  # для Mac с Homebrew
# или
sudo service postgresql start   # для Linux

# Создайте базу данных
psql -U postgres -c "CREATE DATABASE french_course;"
```

### На Windows:

```bash
# Запустите PostgreSQL (обычно запускается автоматически)
# Если нет - найдите "pgAdmin" в меню Пуск

# Создайте базу данных через командную строку:
psql -U postgres -c "CREATE DATABASE french_course;"
```

---

## Шаг 3: Создайте .env файл

Скопируйте файл с примером:

```bash
cp .env.example .env
```

Или создайте вручную файл `.env` со следующим содержимым:

```env
# Server Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=french_course
DB_USER=postgres
DB_PASSWORD=ваш_пароль_от_postgres

# JWT Secret
JWT_SECRET=dev-secret-key-change-in-production-12345

# Остальные переменные (можно оставить пустыми для локального запуска)
YUKASSA_SHOP_ID=
YUKASSA_SECRET_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
```

⚠️ **Важно:** Замените `ваш_пароль_от_postgres` на реальный пароль от вашей PostgreSQL.

---

## Шаг 4: Запустите миграции

Создайте таблицы в базе данных:

```bash
npm run migrate
```

Должны увидеть:
```
🔄 Running database migrations...
✅ Database connected successfully
✅ Migrations completed successfully
```

---

## Шаг 5: Заполните базу тестовыми данными

```bash
npm run seed
```

Должны увидеть:
```
🌱 Seeding database...
✅ Course created with ID: 1
✅ 10 lessons created
🎉 Database seeded successfully
```

---

## Шаг 6: Запустите сервер

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Обычный режим:
```bash
npm start
```

Должны увидеть:
```
🚀 Server is running on http://localhost:3000
📚 French Course Platform API
✅ Database connected successfully
```

---

## Шаг 7: Откройте в браузере

Теперь откройте:

- **Главная страница:** http://localhost:3000/index.html
- **Страница оплаты:** http://localhost:3000/payment.html
- **Вход в кабинет:** http://localhost:3000/login.html
- **Проверка API:** http://localhost:3000/api/health

---

## 🛑 Остановка сервера

Чтобы остановить сервер:
- Нажмите **Ctrl + C** в терминале

---

## ⚠️ Возможные проблемы:

### PostgreSQL не запускается

**На Mac:**
```bash
brew services restart postgresql
```

**На Linux:**
```bash
sudo service postgresql restart
```

**На Windows:**
- Откройте "Службы" (Services)
- Найдите "PostgreSQL"
- Нажмите "Запустить"

### Ошибка подключения к базе данных

Проверьте:
1. PostgreSQL запущен
2. Пароль в `.env` правильный
3. База данных `french_course` создана

### Порт 3000 уже занят

Измените порт в `.env`:
```env
PORT=3001
```

И откройте: http://localhost:3001

---

## 🎉 Готово!

Теперь проект работает локально на вашем компьютере!

Для остановки работы:
1. Нажмите **Ctrl + C** в терминале
2. При необходимости остановите PostgreSQL

Для повторного запуска:
```bash
npm run dev
```

---

## 📝 Полезные команды:

```bash
# Просмотр логов базы данных
psql -U postgres french_course

# Просмотр таблиц
psql -U postgres french_course -c "\dt"

# Просмотр данных в таблице users
psql -U postgres french_course -c "SELECT * FROM users;"

# Пересоздать базу с нуля
psql -U postgres -c "DROP DATABASE french_course;"
psql -U postgres -c "CREATE DATABASE french_course;"
npm run migrate
npm run seed
```

---

Если возникнут проблемы - дайте знать!
